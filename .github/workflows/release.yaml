name: Build and Release Extension

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Create extension package
      run: |
        mkdir -p dist
        zip -r dist/extension.zip . \
          -x "*.git*" \
          -x "*.github*" \
          -x "*.vscode*" \
          -x "node_modules/*" \
          -x "dist/*" \
          -x ".eslintrc*" \
          -x "*.md" \
          -x "*.yaml" \
          -x "*.yml"
        
    - name: Create Release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: dist/extension.zip
        body: |
          🚀 Автоматически собранное расширение для Яндекс Поиска
          
          ### Что нового в этой версии?
          ${{ github.event.head_commit.message }}
          
          ### Установка:
          1. Скачайте `extension.zip`
          2. Распакуйте архив
          3. Перейдите в `chrome://extensions/`
          4. Включите "Режим разработчика"
          5. Нажмите "Загрузить распакованное расширение"
          6. Выберите папку с распакованным расширением
          
          ### Функции:
          - Поиск по сайту с фильтрами
          - Реальное время генерации ссылок
          - Сохранение запросов
          - Автоматические обновления
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
